
<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=0.5, maximum-scale=2.0, user-scalable=no" /> 
<title>placeholder Demo</title>
</head>

<body>
<style>
     input[type=text], input[type=password] {
        border-color: #bbb;
        height: 38px;
        font-size: 14px;
        border-radius: 5px;
        outline: 0;
        border: #ccc 1px solid;
        padding: 0 10px;
        width: 200px;
        -webkit-transition: box-shadow .5s;
        margin-bottom: 15px;
    }
     input[type=code]{
	 border-color: #bbb;
        height: 38px;
        font-size: 14px;
        border-radius: 5px;
        outline: 0;
        border: #ccc 1px solid;
        padding: 0 10px;
        width: 50px;
        -webkit-transition: box-shadow .5s;
	}
        input[type=text]:hover,  input[type=text]:focus, input[type=code]:hover,  input[type=code]:focus {
            border: 1px solid #56b4ef;
            box-shadow: inset 0 1px 3px rgba(0,0,0,.05),0 0 8px rgba(82,168,236,.6);
            -webkit-transition: box-shadow .5s;
        }
    input::-webkit-input-placeholder {
        color: #999;
        -webkit-transition: color .5s;
    }

    input:focus::-webkit-input-placeholder,  input:hover::-webkit-input-placeholder {
        color: #c2c2c2;
        -webkit-transition: color .5s;
    }
</style>
	<form>
		<input type="text" placeholder="邮件地址" name="username" />
		<button  id="get_code" data-role="none">获取验证码</button>			<br/>
		<input type="code"  placeholder="验证码" name="code" align=left>
		<button id="confirm" data-role="none">绑定邮箱</button>
	</form>

	 <script>
		 $("#get_code").click(get_code);
		function get_code()
            {
		<%
		    UserMailer.send_mail("绑定邮箱验证码","469298511@qq.com","您的验证码为：").deliver		    
			  %>
		alert("您的验证邮件已发送，请查收")	
            }
	</script>
</body>
</html>

